# Internal API key — clients must send this as Bearer token
INTERNAL_API_KEY=changeme-internal-key

# Upstream LLM provider (OpenRouter / OpenAI / Anthropic)
UPSTREAM_BASE_URL=https://openrouter.ai/api
UPSTREAM_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxx

# Redis (override only if running outside docker-compose)
# REDIS_URL=redis://localhost:6379/0

# Daily budget limit per user in USD
DAILY_BUDGET_LIMIT=5.0
